{
  "name": "smart-edge-enterprise",
  "version": "1.0.0",
  "private": true,
  "description": "SMART EDGE — Pakistan mobile accessories e-commerce (client + server)",
  "scripts": {
    "dev": "concurrently -n server,client -c blue,cyan \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "build": "npm run build --prefix server && npm run build --prefix client",
    "lint": "npm run lint --prefix server && npm run lint --prefix client",
    "typecheck": "npm run typecheck --prefix server && npm run typecheck --prefix client",
    "test": "npm run test --prefix server",
    "seed": "npm run seed --prefix server",
    "create-admin": "npm run create-admin --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.2.4"
  },
  "engines": {
    "node": ">=20"
  }
}
